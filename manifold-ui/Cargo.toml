[package]
name = "manifold-ui"
version = "0.1.0"
edition = "2021"
description = "Shared design tokens, CSS primitives, and ES-module JS utilities for the Manifold suite frontends. No build step. No framework."

[lib]
name = "manifold_ui"
path = "src/lib.rs"
